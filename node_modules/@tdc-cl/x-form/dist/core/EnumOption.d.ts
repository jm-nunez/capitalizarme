import { InputProps, LabelProps } from '../utils/htmlProps';
import { ValueObject } from 'immutable';
import { Being } from './Being';
export interface EnumOption {
    readonly value: string;
    readonly label: string;
}
export declare class Option implements EnumOption, ValueObject, Being {
    readonly value: string;
    readonly label: string;
    constructor(option: EnumOption);
    toJSON(): string;
    equals(other: any): boolean;
    hashCode(): number;
    is(s: string): boolean;
}
export declare class OptionWithProps extends Option {
    readonly option: Option;
    readonly inputProps: InputProps;
    readonly labelProps: LabelProps;
    constructor(option: Option, inputProps: InputProps, labelProps: LabelProps);
}
