import { Decimal } from 'decimal.js';
import { Result } from '../core/Result';
import { InputProps } from '../utils/htmlProps';
import { XFormLocale } from '../core/XFormLocale';
export declare const decimal: import("../core/CustomField").I$CustomField<string, Decimal>;
/** Function that infers how many decimals are allowed
 * in a numeric field from the inputProps.
 *
 * For example, a step of 0.01 will result in 2 decimals allowed.
 *
 * It returns undefined if the number of allowed decimals cannot be inferred. */
export declare function inferAllowedDecimalsFrom(inputProps: InputProps): number | undefined;
export declare function parseDecimal(input: string, allowedDecimals: number, min: Decimal, max: Decimal, nonZero: boolean, allowNegative: boolean, locale: XFormLocale): Result<Decimal>;
