import { ReactElement } from 'react';
import { $Submitter, OnValid, SubmitConfig } from './$Submitter';
export interface ButtonSpec {
    readonly config: SubmitConfig;
}
export declare function button(config: SubmitConfig): $Submitter;
export declare function button(label: string, config: SubmitConfig): $Submitter;
export declare function button(label: string, onValid: OnValid): $Submitter;
export declare function button(label: ReactElement, config: SubmitConfig): $Submitter;
export declare function button(label: ReactElement, onValid: OnValid): $Submitter;
