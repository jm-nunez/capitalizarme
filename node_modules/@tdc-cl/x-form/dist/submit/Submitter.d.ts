import { ButtonProps } from '../utils/htmlProps';
import { ReactElement } from 'react';
import { SubmitConfig } from './$Submitter';
import { Form } from '../Form';
import { setForm } from '../fields/Field';
interface ISubmitter {
    readonly config: SubmitConfig;
    readonly buttonProps: ButtonProps;
    render(): ReactElement;
    submit(form: Form): Promise<void>;
}
export declare class Submitter implements ISubmitter {
    readonly config: SubmitConfig;
    constructor(config: SubmitConfig);
    private _form;
    get form(): Form;
    [setForm](form: Form): void;
    private _buttonProps?;
    get buttonProps(): ButtonProps;
    private computeButtonProps;
    render(): ReactElement;
    submit(form: Form): Promise<void>;
    private readonly onValid;
    private onInvalid;
    private static defaultOnInvalid;
    private onError;
    private static defaultOnError;
}
export {};
