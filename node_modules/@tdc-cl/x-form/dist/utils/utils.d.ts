import { ReactElement } from 'react';
/** Because we can't do `x instanceof ReactElement`. */
export declare function isReactElement(x: any): x is ReactElement;
export declare function isEmptyOrWhitespace(input: string): boolean;
export declare function removeExcessWhitespace(input: string): string;
export declare function quote(s: string, preferredQuote?: string): string;
export declare function scrollToTop(): void;
